include: package:flutter_lints/flutter.yaml

linter:
  rules:
    prefer_const_constructors: true
    prefer_final_locals: true
    avoid_print: true

analyzer:
  errors:
    # Allow TODO comments (notification wiring is intentionally deferred).
    todo: ignore
